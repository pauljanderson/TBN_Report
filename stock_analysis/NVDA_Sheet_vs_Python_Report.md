# NVDA: Google Sheet vs `rocket_brt` (local `data/newdata/data/NVDA.csv`)

Generated by `compare_nvda_sheet_vs_python.py`.

## Run 1 — `BRTConfig()` defaults

- `touch_threshold=2`, `strong_pivots_enabled=True`, `strong_pivot_mode=pre` (sheet AE-style pre-strong).
- **Python closed trades: 4** | **Sheet rows: 15**

### A) Each Python trade → closest unused sheet row (by entry date + price)

| Py# | Tag   | Python entry | Sheet# | Sheet entry | Match quality |
|-----|-------|--------------|--------|-------------|----------------|
| 1   | LOOSE | 2019-06-05   | 2      | 2019-06-04  | Same win, exit date OK; entry off 1d; days 49 vs 50 |
| 2   | DIFF  | 2022-06-21   | 8      | 2022-06-14  | Same direction (loss) but different %, dates, entry px |
| 3   | DIFF  | 2022-08-05   | 10     | 2022-08-08  | Different dates/prices/days; both losses |
| 4   | LOOSE | 2022-12-29   | 12     | 2022-12-28  | Same 22% win; entry off 1d; days 19 vs 20 |

### B) Sheet rows with **no** Python trade (11 rows)

Sheet #1, 3, 4, 5, 6, 7, 9, 11, 13, 14, 15 — i.e. most of the sheet does not appear under **strong pre** with this data.

### C) Why counts differ

Strong pre-filter removes most pivot touches; only **4** closed trades remain vs **15** on the sheet (which was built without that gate or with different rules).

---

## Run 2 — `strong_pivots_enabled=False` (not default; for exploration)

- **Python: 20** closed trades | **Sheet: 15** rows → **5 extra** Python trades, no empty sheet rows in greedy map.

Row-by-row dates/prices still **do not** align in calendar order (sheet trade #1 is not the same period as Python trade #1). The sheet and this Python path use different entry/exit timing even when counts are closer.

---

## How to re-run

```bash
cd stock_analysis
python compare_nvda_sheet_vs_python.py
```
