@echo off
rem DEPRECATED — symbol lists and PER_SYMBOL_SETTINGS now live in each run_*.bat.
rem Kept so old docs / habits that call this file do not break.
rem Prefer editing the relevant run_???.bat directly (BRT_SYMBOLS, RL_SYMBOLS, etc.).
exit /b 0
